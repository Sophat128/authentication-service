server:
  port: 8086
spring:

#  security:
#    oauth2:
#      resourceserver:
#        jwt:
#          issuer-uri: https://keycloak-fintrack.sythorng.site/auth/realms/Fintrack
#          jwk-set-uri: https://keycloak-fintrack.sythorng.site/auth/realms/Fintrack/protocol/openid-connect/certs

  application:
    name: transaction-service
  kafka:
    producer:
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      bootstrap-servers: localhost:9092
    dead_letter_topic: notification-retry

  datasource:
    url: jdbc:postgresql://localhost:33007/mini_project_db
    username: my_user
    password: my_password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  #hibernate:
  #  jdbc:
  logging:
    level:
      com.levi.testhibernate: debug

kafka:
#  username: todo-username
#  password: todo-password
#  servers: ${KAFKA_SERVER:localhost:9092}
#  idempotence: true
#  acks: all
#  maxInFlightReq: 5
  group-id: hero-consumer
  auto-offset: earliest
#  mechanism: PLAIN
#  protocol: SASL_PLAINTEXT
  topics:
    data: transaction



springdoc:
  api-docs:
    path: /transaction-service/v3/api-docs #these urls will used to load the swagger ui of this service by the api gateway
#  swagger-ui:
#    path: /transaction-service/swagger-ui.html

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/